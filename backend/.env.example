# Port to run the backend server on
PORT=5005

# MongoDB Connection URI (omit or leave empty to use local fallback JSON database)
MONGO_URI=mongodb://localhost:27017/rentease

# Secret key for signing JSON Web Tokens (JWT)
JWT_SECRET=your_jwt_secret_key_here
